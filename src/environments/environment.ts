export const environment = {
  //here the actual swagger url will be http://localhost:5254/api/v1. but this api/v1 will automatically be added by the swagger
  // so we need to remove it from the url
  apiBaseUrl: 'http://localhost:5254'
};
